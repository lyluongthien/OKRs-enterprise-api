export enum TableName {
  User = 'users',
  Objective = 'objective',
  Teams = 'teams',
  JobPosition = 'job_position',
  Role = 'roles',
}

export enum DbConfig {
  DB_TYPE = 'postgres',
  DB_HOST = 'DB_HOST',
  DB_USER = 'DB_USER',
  DB_PASSWORD = 'DB_PASSWORD',
  DB_NAME = 'DB_NAME',
  DB_PORT = 'DB_PORT',
}
